# Monte Carlo and Validation

Validation under friction variation, sensor noise, model mismatch, initial-condition variation, and controller uncertainty. Keep sampled scenarios in `configs/` and generated outputs in `results/`.
